@extends('layouts.app') @section('title',isset($expense)?'Edit Pengeluaran':'Tambah Pengeluaran') @section('content')<form method="post" action="{{isset($expense)?route('expenses.update',$expense):route('expenses.store')}}" class="max-w-xl space-y-4">@csrf @if(isset($expense)) @method('PUT') @endif @foreach([['title','Judul'],['amount','Nominal'],['expense_date','Tanggal']] as [$n,$l])<label class="block"><span class="text-xs text-slate-400">{{$l}}</span><input name="{{$n}}" value="{{old($n,$expense->$n??now()->format('Y-m-d'))}}" class="mt-1 w-full rounded-xl border bg-transparent p-3"></label>@endforeach<textarea name="description" placeholder="Deskripsi" class="w-full rounded-xl border bg-transparent p-3">{{old('description',$expense->description??'')}}</textarea><button class="rounded-xl bg-cyan-500 px-5 py-3 font-bold">Simpan</button></form>@endsection